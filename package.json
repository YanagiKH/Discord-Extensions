{
  "name": "discord-extensions",
  "version": "0.2.0",
  "private": true,
  "description": "Discord Extensions cross-platform companion app and plugin host",
  "author": "YanagiKH",
  "license": "MIT",
  "main": "dist/main/index.js",
  "scripts": {
    "dev": "electron-vite dev",
    "start:desktop": "electron-vite dev",
    "build": "electron-vite build",
    "preview": "electron-vite preview",
    "typecheck": "tsc --noEmit",
    "validate:repo": "node scripts/validate-repository.mjs",
    "validate:browser-extension": "node scripts/validate-browser-extension.mjs",
    "validate:android": "node scripts/validate-android-project.mjs",
    "validate:workbench": "node scripts/validate-workbench.mjs",
    "validate:readmes": "node scripts/validate-readme-parity.mjs",
    "dist": "npx --yes electron-builder@26.0.12 --publish never",
    "dist:win": "npm run build && npx --yes electron-builder@26.0.12 --win nsis --x64 --publish never",
    "dist:mac": "npm run build && npx --yes electron-builder@26.0.12 --mac dmg --x64 --arm64 --publish never",
    "dist:linux": "npm run build && npx --yes electron-builder@26.0.12 --linux AppImage --x64 --publish never"
  },
  "dependencies": {
    "adm-zip": "^0.5.16"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "electron": "^32.0.0",
    "electron-vite": "^2.3.0",
    "typescript": "^5.8.2"
  }
}
