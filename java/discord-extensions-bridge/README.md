# Discord Extensions Bridge

This Maven module is a Java helper layer for plugin profile logic and future native integrations.

It currently provides:

- a safe volume profile model
- volume normalization helpers
- a compile-ready Java 17 Maven setup

It is intentionally separate from the Electron UI so the project can grow into a multi-language plugin platform.
